# agentic-sdw

Bug Report Triage Service — see [`service/README.md`](service/README.md) for
architecture, setup, and testing.

This delivery is a `git bundle`: PR #25's description (the disclosure this
brief asks for) and its comment thread, along with the planning tickets the
service README cites, live in Gitea's own database and do not survive a
bundle/clone. [`docs/what-the-agent-got-wrong.md`](docs/what-the-agent-got-wrong.md)
carries that same disclosure narrative in a form that ships with the repo.
